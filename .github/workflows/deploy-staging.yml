name: Deploy Staging

on:
  push:
    branches: [develop]
  workflow_dispatch:

concurrency:
  group: deploy-staging
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Build frontend
        run: |
          npm ci
          npm run build

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          port: 21
          server-dir: /domains/dombicenter.com/public_html/staging/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
            **/tests/**
            **/docs/**
            **/.env
            **/.env.example
            **/.env.staging.example
            **/.env.production.example
            **/.github/**
            **/.agents/**
            **/.claude/**
            **/.superpowers/**
            **/.opencode/**
            **/.codex/**
            **/.phpunit.cache/**
            **/storage/logs/**
            **/storage/framework/sessions/**
            **/storage/framework/cache/**
            **/storage/framework/views/**
            .editorconfig
            .eslintrc.cjs
            .npmrc
            .prettierrc
            .prettierignore
            phpunit.xml
            pint.json
            tsconfig.json
            vite.config.ts
            vitest.config.ts
            README.md
            DESIGN.md
            STITCH_PROMPTS.md
            skills-lock.json
            pnpm-lock.yaml
            pnpm-workspace.yaml
            package-lock.json

      - name: Post-deploy (composer + migrate + cache)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          port: 21
          server-dir: /domains/dombicenter.com/public_html/staging/
          local-dir: ./deploy-trigger/
          exclude: |
            **/*
