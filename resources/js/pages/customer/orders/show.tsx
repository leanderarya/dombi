import { Head, router } from '@inertiajs/react';
import CustomerLayout from '../../../layouts/customer-layout';

export default function OrderShow({ order }: any) {
    return <CustomerLayout><Head title={order.order_code} /><div className="rounded-lg border bg-white p-5"><div className="flex items-start justify-between gap-4"><div><h1 className="text-xl font-semibold">{order.order_code}</h1><div className="mt-1 text-sm text-zinc-500">Outlet: {order.outlet?.name ?? '-'}</div></div><span className="rounded-full bg-amber-100 px-3 py-1 text-xs text-amber-800">{order.status}</span></div><div className="mt-5 border-t pt-4 text-sm"><div className="font-medium">{order.customer_name}</div><div>{order.customer_phone}</div><div className="text-zinc-600">{order.customer_address}</div></div><div className="mt-5 space-y-3">{order.items.map((item: any) => <div key={item.id} className="flex justify-between border-t pt-3 text-sm"><div><div className="font-medium">{item.product_name}</div><div className="text-zinc-500">Qty {item.quantity} x Rp {Number(item.price).toLocaleString('id-ID')}</div></div><div className="font-medium">Rp {Number(item.subtotal).toLocaleString('id-ID')}</div></div>)}</div><div className="mt-5 border-t pt-4 text-right"><div className="text-sm text-zinc-500">Total</div><div className="text-2xl font-semibold">Rp {Number(order.total).toLocaleString('id-ID')}</div></div><button onClick={() => router.post(`/customer/orders/${order.id}/repeat`)} className="mt-5 w-full rounded-md border px-4 py-2">Order ulang</button></div></CustomerLayout>;
}
